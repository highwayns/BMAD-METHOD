version: 1
job:
  name: "{{job_name|batch-scoring}}"
  schedule_cron: "{{cron|0 2 * * *}}"
  inputs:
    table: "{{input_table}}"
    model_uri: "{{model_uri}}"
  outputs:
    table: "{{output_table}}"
  cluster:
    policy_id: "{{cluster_policy_id}}"
    pool_id: "{{pool_id}}"
