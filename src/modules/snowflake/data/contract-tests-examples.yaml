contract:
  schema:
    - name: customers
      required_columns: [id, email, created_at]
  quality:
    - table: customers
      rule: null_rate(email) < 0.5%
  semantics:
    - metric: revenue
      definition_hash: 9c12aa
