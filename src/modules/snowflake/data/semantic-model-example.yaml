dimensions:
  - name: dim_user
    pk: user_id
    scd: type2
measures:
  - name: m_orders
    agg: sum
  - name: m_revenue
    agg: sum
policies:
  - mask: email -> masked unless role in ['PII_READER']
