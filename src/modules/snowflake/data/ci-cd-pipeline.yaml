name: mlops
on: [push]
jobs:
  build:
    steps:
      - uses: actions/checkout@v3
      - name: run tests
        run: echo "tests placeholder"
      - name: build artifacts
        run: echo "build placeholder"
