-- Example Dynamic Table and refresh policy
CREATE OR REPLACE DYNAMIC TABLE SILVER_ORDERS
  TARGET_LAG = '60 MINUTES'
  WAREHOUSE = WH_ELT
  AS SELECT * FROM BRONZE_ORDERS WHERE status <> 'CANCELLED';
