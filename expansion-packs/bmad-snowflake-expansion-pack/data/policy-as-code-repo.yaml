repo:
  name: platform-policies
  structure:
    - policies/
    - templates/
    - tests/
    - pipelines/
  pipelines:
    - pre-merge: lint + unit + contract-tests
    - release: tag + apply + verify + rollback
