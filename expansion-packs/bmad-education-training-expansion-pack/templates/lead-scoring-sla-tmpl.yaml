template:
  id: lead-scoring-sla
  title: 线索评分与 SLA
  sections:
    - id: signals
      title: 评分信号
      type: table
      columns: [Signal, Weight, Threshold, Decay, Note]
      elicit: true
    - id: routing
      title: 路由与SLA
      type: table
      columns: [State, Owner, SLA, Next Step, Escalation]
      elicit: true
