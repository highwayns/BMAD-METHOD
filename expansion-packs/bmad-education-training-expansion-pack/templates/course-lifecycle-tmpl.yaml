template:
  id: course-lifecycle
  title: 课程生命周期与发布
  sections:
    - id: stages
      title: 阶段与闸口
      type: table
      columns: [Stage, Gate, Owner, Checks, Rollback]
      elicit: true
    - { id: versioning, title: 版本与回滚, instruction: "快照/对比/回退" }
