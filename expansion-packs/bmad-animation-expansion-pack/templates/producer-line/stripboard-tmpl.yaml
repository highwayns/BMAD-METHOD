version: 1
template_id: producer-line/stripboard
purpose: Stripboard 片段化排期
fields:
  week: { type: string, example: "2025-W37" }
  strips:
    type: table
    columns: [seq, shot, duration, dept, owner, start, end, location, notes]
outputs:
  - schedule/stripboard-{{week}}.yaml
