version: 1
template_id: post-supervisor/delivery-manifest
purpose: 交付包清单
fields:
  target: { type: string }
  items:
    type: table
    columns: [item, path, container, checksum]
  metadata:
    type: table
    columns: [name, value]
outputs:
  - delivery/delivery-{{target}}.yaml
