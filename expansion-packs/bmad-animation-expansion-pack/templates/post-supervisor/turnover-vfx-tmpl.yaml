version: 1
template_id: post-supervisor/turnover-vfx
purpose: VFX 交接包
fields:
  seq: { type: string }
  plates:
    type: table
    columns: [shot, path, codec, fps, tc_in, tc_out, checksum]
  references:
    type: table
    columns: [type, path, checksum]
  notes: { type: list, items: string }
outputs:
  - turnovers/vfx-{{seq}}.yaml
