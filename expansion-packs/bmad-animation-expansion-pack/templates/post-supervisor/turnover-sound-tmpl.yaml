version: 1
template_id: post-supervisor/turnover-sound
purpose: 声音交接包
fields:
  seq: { type: string }
  elements:
    type: table
    columns: [type, path, format, sample_rate, bitdepth, channels, checksum]
  adr_vo:
    type: table
    columns: [actor, role, date, takes, notes]
  deliverables:
    type: list
    items: string
outputs:
  - turnovers/sound-{{seq}}.yaml
