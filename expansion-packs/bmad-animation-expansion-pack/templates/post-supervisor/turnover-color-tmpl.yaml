version: 1
template_id: post-supervisor/turnover-color
purpose: 调色交接
fields:
  seq: { type: string }
  color_refs:
    type: table
    columns: [type, path, colorspace, view, lut, checksum]
  monitors:
    type: table
    columns: [model, serial, calib_date, delta_e]
  notes: { type: list, items: string }
outputs:
  - turnovers/color-{{seq}}.yaml
