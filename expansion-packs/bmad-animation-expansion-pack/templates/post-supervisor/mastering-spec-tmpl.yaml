version: 1
template_id: post-supervisor/mastering-spec
purpose: 母版规格
fields:
  target: { type: string }
  picture:
    type: table
    columns: [resolution, fps, container, codec, bitdepth, colorspace, transfer]
  audio:
    type: table
    columns: [layout, sample_rate, bitdepth, loudness_target, true_peak]
  captions:
    type: table
    columns: [standard, format, language]
outputs:
  - specs/mastering-{{target}}.yaml
