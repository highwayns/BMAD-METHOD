version: 1
template_id: modeling-lead/td-targets-sheet
purpose: Texel Density 目标表
fields:
  asset: { type: string }
  targets:
    type: table
    columns: [zone, td_px_per_m, min, max]
outputs:
  - reports/td-targets-{{asset}}.md
