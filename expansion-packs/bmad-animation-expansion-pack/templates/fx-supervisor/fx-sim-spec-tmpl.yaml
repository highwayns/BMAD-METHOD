version: 1
template_id: fx-supervisor/fx-sim-spec
purpose: 求解器/参数/单位 规范
fields:
  show: { type: string }
  scale:
    type: table
    columns: [unit, gravity_mps2, meter_per_scene_unit]
  solvers:
    type: table
    columns: [domain, solver, dt, substeps, voxel, iterations, notes]
  caches:
    type: table
    columns: [type, container, path_token, checksum]
outputs:
  - specs/fx-sim-spec-{{show}}.yaml
