version: 1
template_id: fx-supervisor/aov-contract
purpose: FX AOV/ID 合同
fields:
  preset: { type: string }
  aovs:
    type: table
    columns: [name, bitdepth, colorspace, premult, notes]
  order:
    type: list
    items: string
outputs:
  - comp/aov-contract-{{preset}}.yaml
