version: 1
template_id: fx-supervisor/comp-pack
purpose: FX 合成套餐
fields:
  preset: { type: string }
  crypto:
    type: table
    columns: [type, enabled, notes]
  id_masks:
    type: table
    columns: [name, rule, color]
  nuke_templates:
    type: list
    items: string
outputs:
  - comp/comp-pack-{{preset}}.yaml
