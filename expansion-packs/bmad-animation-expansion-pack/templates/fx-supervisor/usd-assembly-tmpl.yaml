version: 1
template_id: fx-supervisor/usd-assembly
purpose: USD FX 装配
fields:
  seq: { type: string }
  volumes:
    type: table
    columns: [prim, vdb_path, density, temperature, vel]
  points:
    type: table
    columns: [prim, abc_path, width, purpose]
  variants:
    type: table
    columns: [set, name, purpose]
outputs:
  - usd/usd-fx-assembly-{{seq}}.yaml
