version: 1
template_id: art-director/palette
purpose: 项目配色/设计令牌
fields:
  scheme: { type: string, example: "WARM_DAWN" }
  tokens:
    type: table
    columns: [token, kind, value, usage]
outputs:
  - lookdev/palette-{{scheme}}-{{date}}.yaml
