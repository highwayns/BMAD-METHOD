version: 1
template_id: anim-supervisor/playblast-pack
purpose: Playblast 打包规范
fields:
  naming: { type: string, example: "{PROJECT}_{SEQ}_{SHOT}_PB_{PASS}_v###.mp4" }
  burnins: { type: table, columns: [pos, text, font, size] }
  watermark: { type: table, columns: [text, opacity, ttl_days] }
  encoding: { type: table, columns: [container, vcodec, bitrate, fps, colorspace] }
outputs:
  - specs/playblast-pack.yaml
