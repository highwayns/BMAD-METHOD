version: 1
template_id: lighting-lead/set-light-assembly
purpose: USD 灯光装配与 Light Linking
fields:
  seq: { type: string }
  layers: { type: table, columns: [layer, purpose, payload, notes] }
  lights: { type: table, columns: [prim, type, intensity, exposure, color, link_rule] }
  linking: { type: table, columns: [set, include, exclude, notes] }
outputs:
  - usd/set-light-assembly-{{seq}}.yaml
