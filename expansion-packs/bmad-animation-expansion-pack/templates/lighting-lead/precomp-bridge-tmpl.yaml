version: 1
template_id: lighting-lead/precomp-bridge
purpose: 预合成桥接包
fields:
  preset: { type: string }
  rebuild:
    type: table
    columns: [aov, op, weight, notes]
  nuke_templates:
    type: list
    items: string
outputs:
  - bridges/precomp-{{preset}}.yaml
