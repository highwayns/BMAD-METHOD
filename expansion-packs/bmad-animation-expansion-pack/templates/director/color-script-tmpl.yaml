version: 1
template_id: director/color-script
purpose: 色彩脚本（场景情绪/时间/灯光/调色）
fields:
  scenes:
    type: table
    columns: [scene, time_of_day, mood, palette, key_light, fill, rim, notes]
outputs:
  - lookdev/color-script-{{date}}.yaml
