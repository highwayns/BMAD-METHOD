version: 1
template_id: director/camera-plan
purpose: 机位/运动/镜头语言计划
fields:
  seq: { type: string }
  plan:
    type: table
    columns: [shot, shot_type, lens, move, framing, duration, purpose]
outputs:
  - shoot/camera-plan-{{seq}}.yaml
