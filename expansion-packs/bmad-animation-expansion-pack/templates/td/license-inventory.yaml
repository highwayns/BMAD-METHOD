version: 1
template_id: td/license-inventory
purpose: 许可证清单
fields:
  licenses:
    type: table
    columns: [product, version, seats, expiry, owner, notes]
outputs:
  - configs/license-inventory.yaml
