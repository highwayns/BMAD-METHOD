version: 1
template_id: td/storage-forecast-model
purpose: 存储/带宽预测模型
fields:
  assumptions:
    type: table
    columns: [metric, value, notes]
  outputs:
    type: table
    columns: [week, storage_tb, egress_tb]
outputs:
  - analytics/storage-forecast-model.yaml
