version: 1
template_id: td/publish-schema
purpose: 发布 Schema（资产/镜头的family、元数据、路径规则）
fields:
  families:
    type: table
    columns: [family, subtype, ext, preview, required_meta]
  path_tokens:
    type: table
    columns: [token, example, desc]
  metadata_schema:
    type: table
    columns: [key, type, required, desc]
outputs:
  - specs/publish-schema.yaml
