version: 1
template_id: cg-supervisor/render-preflight
purpose: 渲染预检参数表
fields:
  renderer: { type: string }
  aov:
    type: list
    items: string
  outputs:
    type: table
    columns: [path, bitdepth, compression, colorspace]
  checks:
    type: table
    columns: [check, expected, actual]
outputs:
  - reports/render-preflight.yaml
