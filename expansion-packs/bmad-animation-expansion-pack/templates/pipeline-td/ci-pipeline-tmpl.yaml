version: 1
template_id: pipeline-td/ci-pipeline
purpose: CI/CD 流水线模板
fields:
  stages: { type: list, items: string }
  jobs:
    type: table
    columns: [name, stage, script, artifacts]
outputs:
  - ci/ci-pipeline.yaml
