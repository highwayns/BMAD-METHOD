# asset-tracker-tmpl.yaml
version: 1
template_id: exec-producer/asset-tracker
purpose: 资产与镜头追踪
fields:
  assets:
    type: table
    columns: [id, type, name, status, owner, version, path]
  shots:
    type: table
    columns: [seq, shot, status, cut_len, latest_ver, notes]
outputs:
  - tracking/asset-tracker-{{date}}.csv
