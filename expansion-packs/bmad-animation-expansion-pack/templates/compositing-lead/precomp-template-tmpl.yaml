version: 1
template_id: compositing-lead/precomp-template
purpose: 预合成模板与节点库
fields:
  preset: { type: string }
  groups:
    type: table
    columns: [name, purpose, path, version]
  nuke_templates:
    type: list
    items: string
outputs:
  - bridges/precomp-{{preset}}.yaml
