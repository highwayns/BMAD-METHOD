version: 1
template_id: lookdev-shading-lead/texture-pipeline
purpose: 纹理管线/UDIM/压缩/缓存
fields:
  asset: { type: string }
  udim:
    type: table
    columns: [tile, coverage_pct, padding_px, notes]
  cache:
    type: table
    columns: [format, mip, compression, colorspace, notes]
  sampling:
    type: table
    columns: [filter, anisotropy, triplanar_fallback, notes]
outputs:
  - specs/texture-pipeline-{{asset}}.yaml
