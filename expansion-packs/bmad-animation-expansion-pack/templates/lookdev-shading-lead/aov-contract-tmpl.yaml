version: 1
template_id: lookdev-shading-lead/aov-contract
purpose: AOV/合成合同
fields:
  preset: { type: string }
  aovs:
    type: table
    columns: [name, bitdepth, colorspace, premult, notes]
  order:
    type: list
    items: string
outputs:
  - comp/aov-contract-{{preset}}.yaml
