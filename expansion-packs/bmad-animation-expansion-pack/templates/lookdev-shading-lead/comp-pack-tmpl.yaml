version: 1
template_id: lookdev-shading-lead/comp-pack
purpose: 合成模板/ID/Mask 套餐
fields:
  preset: { type: string }
  crypto:
    type: table
    columns: [type, enabled, notes]
  id_masks:
    type: table
    columns: [name, rule, color]
  nuke_templates:
    type: list
    items: string
outputs:
  - comp/comp-pack-{{preset}}.yaml
