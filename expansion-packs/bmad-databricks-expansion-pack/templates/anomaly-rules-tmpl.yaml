version: 1
metadata:
  owner: "{{team|data-quality}}"
  env: "{{env|prod}}"
anomaly_rules:
  - id: missing_data_spike
    metric: rows_missing_pct
    threshold: 0.05
    window: 5m
    severity: P2
  - id: value_drift
    metric: js_divergence
    threshold: 0.2
    window: 1h
    severity: P3
