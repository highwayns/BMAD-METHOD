version: 1
contract:
  name: "{{name}}"
  owner: "{{owner}}"
  producers:
    - "{{producer_team}}"
  consumers:
    - "{{consumer_team}}"
  schema:
    columns:
      - name: "{{col1}}"
        type: "{{type1}}"
  slas:
    freshness_min: "{{freshness|30}}"
    availability_pct: "{{avail|99.9}}"
