version: 1
analysis:
  initiative: "{{initiative}}"
  benefits:
    - type: revenue
      amount: "{{benefit_amount}}"
  costs:
    - type: compute
      amount: "{{compute_cost}}"
  roi_formula: (benefits - costs) / costs
