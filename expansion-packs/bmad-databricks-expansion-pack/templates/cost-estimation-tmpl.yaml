version: 1
estimation:
  workload: "{{workload_name}}"
  assumptions:
    hrs_per_day: "{{hours|4}}"
    workers: "{{workers|2}}"
  unit_cost:
    dwu_per_hr: "{{dwu_cost}}"
    storage_per_tb_mo: "{{storage_cost}}"
