resources:
  jobs:
    - name: "{{job_name}}"
      file_path: "{{notebook_or_wheel}}"
  pipelines:
    - name: "{{pipeline_name}}"
      dlt_config: "{{dlt_config_path}}"
  warehouses:
    - name: "{{warehouse_name}}"
      size: "{{size|2X-Small}}"
