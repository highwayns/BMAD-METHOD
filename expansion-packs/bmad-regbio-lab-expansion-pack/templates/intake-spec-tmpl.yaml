intake_spec:
  sources: ["{{instrument}}", "{{uploader}}"]
  checksums: true
  directory_layout: "{{layout}}"
  required_metadata: ["project", "sample_id", "assay", "run_id", "coi", "coc"]
  pii_phi_policy: "{{policy}}"
  acceptance_criteria: "{{criteria}}"
