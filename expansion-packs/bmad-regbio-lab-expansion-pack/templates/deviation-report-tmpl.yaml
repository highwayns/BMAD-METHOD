deviation:
  id: "DV-{{yyyy}}{{seq}}"
  date: "{{date}}"
  description: "{{what_happened}}"
  severity: "{{severity}}"
  immediate_action: "{{containment}}"
investigation:
  root_cause: "{{root_cause}}"
capa_triggered: true
links: ["{{related_change}}", "{{batch_id}}"]
