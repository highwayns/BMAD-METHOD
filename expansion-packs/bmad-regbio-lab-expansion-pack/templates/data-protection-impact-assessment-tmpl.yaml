dpia:
  processing_activity: "{{activity}}"
  lawful_basis: "{{basis}}"
  data_minimization: "{{measures}}"
  access_control: "{{rbac}}"
  transfers: "{{cross_border}}"
  retention: "{{retention_policy}}"
  risks:
    - "{{risk}}"
  mitigations:
    - "{{mitigation}}"
