audit_pack:
  scope: "{{lab_name}} 审计索引"
  sops:
    - id: "{{sop_id}}"
      title: "{{sop_title}}"
  records:
    - path: "{{record_path}}"
      desc: "{{desc}}"
  certificates:
    - type: "Calibration|Training|BSC|PPE"
      file: "{{file}}"
  gaps:
    - risk: "{{risk}}"
      mitigation: "{{mitigation}}"
