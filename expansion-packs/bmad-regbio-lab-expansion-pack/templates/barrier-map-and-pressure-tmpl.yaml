barrier:
  rooms:
    - id: "{{room}}"
      level: "SPF|SPF/BSL2"
      pressure_setpoint_Pa: "{{delta}}"
      ach: "{{air_changes}}"
      flow: "single-pass|recirc with HEPA"
  flows: ["personnel", "materials", "waste"]
