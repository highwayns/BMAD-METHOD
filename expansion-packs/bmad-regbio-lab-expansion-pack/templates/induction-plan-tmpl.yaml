induction:
  tissue: "{{tissue}}"
  stages:
    - day: 0
      factors: ["{{factor}}"]
      oxygen: "{{O2}}"
      shear: "{{shear}}"
      notes: "{{notes}}"
  stop_rules: ["{{stop_rule}}"]
  checkpoints: ["{{marker}}", "{{function}}"]
