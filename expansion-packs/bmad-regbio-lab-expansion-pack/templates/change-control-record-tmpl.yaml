change:
  id: "CC-{{yyyy}}{{seq}}"
  title: "{{title}}"
  reason: "{{reason}}"
  impacted_docs: ["{{doc}}"]
  risk_level: "{{risk}}"
  validation: ["IQ", "OQ", "PQ", "Regression"]
  approvers: ["QA", "Owner", "IT/Validation"]
  effective_date: "{{effective_date}}"
