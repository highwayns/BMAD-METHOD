mlops_validation:
  splits: { train: 0.7, val: 0.15, test: 0.15, external: true }
  metrics: ["AUROC", "AUPRC", "F1", "Calibration"]
  fairness: ["site", "sex", "age"]
  drift: ["PSI", "feature drift", "data drift"]
