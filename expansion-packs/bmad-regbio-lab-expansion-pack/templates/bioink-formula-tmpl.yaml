bioink:
  polymers: ["{{polymer}}"]
  crosslinkers: ["{{crosslinker}}"]
  additives: ["{{additive}}"]
  cell_density: "{{cells_per_ml}}"
  rheology: "{{rheology_targets}}"
  sterility_endotoxin: "{{limits}}"
  storage: "{{storage}}"
