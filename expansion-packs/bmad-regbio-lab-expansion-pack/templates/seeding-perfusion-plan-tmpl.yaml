seeding_perfusion:
  seeding_method: "static|dynamic"
  target_density: "{{cells_per_cm3}}"
  perfusion:
    flow_rate: "{{ml_per_min}}"
    schedule: "{{duty_cycle}}"
    oxygen: "{{DO_target}}"
    ph: "{{pH_target}}"
  uniformity_metrics: ["{{metric}}"]
