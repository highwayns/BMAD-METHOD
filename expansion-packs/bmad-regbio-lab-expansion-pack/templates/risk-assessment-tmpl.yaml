risk_assessment:
  method: "HAZID|COSHH|JHA"
  hazards:
    - source: "{{source}}"
      exposure: "{{route}}"
      severity: "{{sev}}"
      likelihood: "{{lik}}"
      controls:
        - level: "elimination|substitution|engineering|administrative|PPE"
          action: "{{action}}"
  residual_risk: "{{residual}}"
  verification: "{{how_to_verify}}"
