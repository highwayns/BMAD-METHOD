incident:
  id: "INC-{{yyyy}}{{seq}}"
  type: "spill|exposure|needle-stick|chemical|equipment|utility|security"
  date: "{{date}}"
  location: "{{location}}"
  description: "{{what_happened}}"
  immediate_action: "{{containment}}"
  classification: "{{severity}}"
  reporter: "{{name}}"
  attachments: ["{{file}}"]
