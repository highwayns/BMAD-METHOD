sample_intake:
  project: "{{project}}"
  samples:
    - id: "{{sample_id}}"
      type: "DNA|RNA|Cells|Tissue"
      volume: "{{ul}}"
      concentration: "{{ng_per_ul}}"
      condition: "dry ice|ice|LN2"
      received_at: "{{datetime}}"
      anomalies: "{{notes}}"
