sample_receipt_and_fixation:
  project: "{{project}}"
  samples:
    - id: "{{sample_id}}"
      tissue: "{{tissue}}"
      fixative: "10% NBF|PFA|Others"
      ratio_tissue_to_fixative: "1:10"
      time_to_fix: "{{minutes}}"
      fixation_time: "{{hours}}"
      temperature: "{{temp}}"
      anomalies: "{{notes}}"
  coi_coc: true
