deviation:
  id: DV-{{yyyy}}{{seq}}
  date: "{{date}}"
  description: "{{what_happened}}"
  severity: "{{severity}}"
  immediate_action: "{{containment}}"
root_cause:
  method: "5Why"
  analysis: "{{root_cause}}"
capa:
  corrective:
    - "{{corrective_action}}"
  preventive:
    - "{{preventive_action}}"
effectiveness_check: "{{how_to_verify}}"
