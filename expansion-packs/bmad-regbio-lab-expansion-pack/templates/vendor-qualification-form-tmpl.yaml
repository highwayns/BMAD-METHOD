vendor:
  name: "{{vendor}}"
  certificates: ["ISO9001", "ISO13485", "GMP"]
  audit_score: "{{score}}"
  quality_agreement: "{{qa_status}}"
  change_notification: "{{policy}}"
  nonconformance_rate: "{{ncr}}"
