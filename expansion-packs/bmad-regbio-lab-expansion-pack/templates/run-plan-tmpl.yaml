run_plan:
  platform: "Illumina|ONT"
  flowcell: "{{model}}"
  read_length: "{{read}}"
  lanes:
    - lane: 1
      samples: ["{{sample_id}}"]
      spikein_phix: "{{percent}}"
  targets:
    coverage: "{{x}}"
    q30: ">=85%?"
